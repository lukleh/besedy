{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // empty' | grep -qE 'prisma\\s+(db\\s+push|migrate\\s+reset)' && { echo '⛔ BLOCKED: prisma db push/migrate reset detected. If you really need this, run it manually in your terminal.' >&2; exit 2; } || exit 0"
          }
        ]
      }
    ]
  }
}
